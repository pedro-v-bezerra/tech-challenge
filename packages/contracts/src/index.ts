/**
 * Contratos compartilhados entre os serviços (DTOs, payloads de evento, nomes de tópico).
 * Placeholder na Fase 1 — preenchido na Fase 2 (contracts-and-data-model).
 */
export const CONTRACTS_PACKAGE = '@biud/contracts';
