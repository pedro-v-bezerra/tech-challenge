packages:
  - 'apps/*'
  - 'packages/*'

# Dependências autorizadas a rodar build scripts (pnpm bloqueia por padrão, por segurança).
# Prisma precisa posicionar o query engine; esbuild é usado pelo tsx (seed).
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  esbuild: true
  prisma: true
